{
  "name": "cere03",
  "version": "1.0.0",
  "description": "JS utility to deploy contract and call contract functions",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "MM",
  "license": "ISC",
  "dependencies": {
    "@polkadot/api": "^4.1.1",
    "@polkadot/api-contract": "^4.1.1"
  }
}
